/**
 * This is an abstract base class for either a success or error response.
 *
 * See `SuccessResponse` and `ErrorResponse` for implementations.
 *
 * @export
 * @class ResponseValue
 */
export interface ResponseValueInterface {
}
