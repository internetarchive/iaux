# Needed for script use of npm publish 
# to not use https://registry.yarnpkg.com
# when doing
# - yarn publish:alpha
#
# Prefer
# - npm run publish:alpha
# so we can remove this file
#
# Need to be logged in to npm to publish
# - npm login
--registry "https://registry.npmjs.org/"