// Jest times out if execution of its tests takes too long, where "too long" is 5 seconds by default.
// Browser tests usually take longer, so this increases the time to 20 seconds.
jest.setTimeout(20000);
